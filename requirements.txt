fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.7.0
httpx>=0.27.0
numpy>=1.26.0
matplotlib>=3.8.0
python-dotenv>=1.0.1

# Optional Detective backends (lazy-imported; mock works without these)
openai>=1.40.0
anthropic>=0.34.0
huggingface_hub>=0.24.0

# RL / training (not needed to run just the env server)
torch>=2.2.0
transformers>=4.44.0
accelerate>=0.33.0
peft>=0.12.0
trl>=0.10.0
datasets>=2.20.0
bitsandbytes>=0.43.0; platform_system != "Windows"

# Experiment tracking
wandb>=0.17.0
tensorboard>=2.15.0

pytest>=8.0.0
